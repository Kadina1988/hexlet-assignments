en:
  activerecord:
    models:
      # BEGIN
      post: Post
        title: Title
        body: Body
      # END
      post/comment: Comment
    attributes:
      # BEGIN
      title: Title
      body: Body
      # END
      post/comment:
        body: Body

    errors:
      models:
        post:
          attributes:
            # BEGIN
            title:
              blank: should be filed
            # END
